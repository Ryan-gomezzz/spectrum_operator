REPLACE WITH: W&B chart showing auction training curve, total reward over
150 GRPO steps, baseline marker at 0.13, axes labeled "training step" and
"mean reward". Filter out the dispute run.

Save the real image as `auction_reward_curve.png` in this directory and
delete this `.PLACEHOLDER` file.

Capture instructions:
- Open the W&B project for the auction run
- Filter the run list to the auction run only (deselect dispute, coalition)
- Plot: y-axis = `train/reward` (or whichever metric is the total reward),
  x-axis = `train/global_step`
- Add a horizontal reference line at y=0.1374 (the auction baseline) and
  label it "baseline"
- Smoothing: light (0.6 EMA is fine)
- Axis labels: "training step" / "mean reward"
- Title: "Auction reward over training"
- Export at >= 1200px wide PNG
