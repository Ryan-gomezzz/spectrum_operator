REPLACE WITH: full visualization screenshot, mid-episode auction state, all
three columns visible, taken at ~1920x1080 from the deployed Space's
/visualize page.

Save the real image as `visualization_hero.png` in this directory and delete
this `.PLACEHOLDER` file. The README and blog already reference the final
filename — images will render automatically once swapped.

Suggested capture moment:
- Pick the auction task in the task selector
- Click "Start episode", let it run 2-3 rounds
- Pause when oversight events panel has at least one warning, one
  commendation, and one reputation_update
- Reward breakdown panel should show non-zero values across all four
  components
- Window width >= 1600px so all three columns render side-by-side
